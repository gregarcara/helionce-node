---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node:5.11.0-slim

inputs:
  - name: helionce-git-repo
outputs:
  - name: build_code_output

run:
  path: build_code_output/hce-ci/scripts/build
